  <!-- SIDE PANEL: USER DETAIL -->
  <div id="user-detail-overlay" class="hidden fixed inset-0 bg-slate-900/50 z-50" onclick="closeUserDetail(event)">
    <aside id="user-detail-panel" class="absolute top-0 right-0 h-full w-full max-w-md bg-white shadow-2xl flex flex-col translate-x-full transition-transform duration-200" onclick="event.stopPropagation()">
      <div class="flex items-start justify-between gap-3 px-5 py-4 border-b border-slate-200 shrink-0">
        <div class="min-w-0">
          <h3 id="user-detail-name" class="text-lg font-bold text-slate-800 truncate">—</h3>
          <p id="user-detail-subline" class="text-xs text-slate-500 font-mono"></p>
        </div>
        <button onclick="closeUserDetail()" class="text-slate-400 hover:text-slate-600 text-xl font-bold shrink-0">✕</button>
      </div>
      <div id="user-detail-body" class="flex-1 overflow-y-auto px-5 py-4 space-y-5 text-sm">
        <p class="text-slate-400">Loading…</p>
      </div>
    </aside>
  </div>
